//splice()
//cambia el contenido de un array eliminando elementos existentes y/o agregando nuevos

let array = ['jorge', 'maria', 'pedro', 'juan'];

let resultado = array.shift(1, 2); 

document.write(resultado); // resultado = ['jorge']